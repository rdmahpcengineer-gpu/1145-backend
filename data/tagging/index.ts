/**
 * data/tagging — the shared tenant-tagging helper.
 *
 * Public surface reused by the DM-3 embedding store (task 3.2) and the ML-1
 * stream ingest pipeline (task 19.1).
 */
export * from './tenant_tag';
